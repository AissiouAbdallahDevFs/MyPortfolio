{% extends 'base.html.twig' %}

{% block title %}Hello HomePageController!{% endblock %}

{% block body %}
<style>
    .example-wrapper {
        margin: 1em auto;
        max-width: 800px;
        width: 95%;
        font: 18px/1.5 sans-serif;
    }

    .example-wrapper code {
        background: #F5F5F5;
        padding: 2px 6px;
    }
</style>

    <h1>Hello {{ controller_name }}! ✅</h1>
    <img/>
    {% for formation in formations %}
    <div class="card" style="width: 18rem;">
        <div class="card-body">
            <h5 class="card-title">{{ formation.diplome }}</h5>
            <h6 class="card-subtitle mb-2 text-muted">{{ formation.ecole }}</h6>
        </div>
    </div>
{% endfor %}
<h2>Skills</h2>
{% for skill in skills %}
<div class="card" style="width: 18rem;">
    <div class="card-body">
        <h5 class="card-title">{{ skill.Langage }}</h5>
        <h6 class="card-subtitle mb-2 text-muted">{{ skill.Level }}</h6>
    </div>
</div>
{% endfor %}

{% endblock %}